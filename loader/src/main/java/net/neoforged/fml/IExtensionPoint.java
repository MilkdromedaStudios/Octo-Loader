package net.neoforged.fml;

/**
 * The marker a mod registers arbitrary values under, so another mod can read
 * them off its container without either of them knowing about the other.
 */
public interface IExtensionPoint {
}
